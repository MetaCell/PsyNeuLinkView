#!/usr/bin/env bash -l
pip install -vv psyneulinkviewer --break-system-packages --use-pep517 && source ~/.bash_profile && conda activate psyneulinkview && open ~/psyneulinkviewer-darwin-x64/psyneulinkviewer.app/
